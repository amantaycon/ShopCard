server:
  port: 8089

spring:
  application:
    name: profile-service
  datasource:
    url: jdbc:postgresql://localhost:5432/shopcard_profile
    username: shopcard_admin
    password: admin_password_secure_123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
